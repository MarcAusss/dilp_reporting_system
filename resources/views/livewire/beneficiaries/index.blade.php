<div class="space-y-7"><x-ui.page-header title="Beneficiary Registry" eyebrow="DILP Monitoring" description="Search and filter beneficiary records across all DILP projects without duplicating beneficiary data." />
<div class="grid gap-4 sm:grid-cols-3"><x-ui.card><p class="text-[10px] font-bold uppercase text-slate-400">Records</p><p class="mt-3 text-2xl font-bold">{{ number_format($summary['total']) }}</p></x-ui.card><x-ui.card><p class="text-[10px] font-bold uppercase text-slate-400">Active</p><p class="mt-3 text-2xl font-bold text-emerald-700">{{ number_format($summary['active']) }}</p></x-ui.card><x-ui.card><p class="text-[10px] font-bold uppercase text-slate-400">Projects Represented</p><p class="mt-3 text-2xl font-bold text-[#164b73]">{{ number_format($summary['projects']) }}</p></x-ui.card></div>
<x-ui.card :padding="false"><div class="border-b border-slate-100 px-5 py-5"><div class="grid gap-3 md:grid-cols-4"><input type="search" wire:model.live.debounce.300ms="search" placeholder="Search beneficiary or project..." class="rounded-lg border border-slate-300 px-3 py-2.5 text-sm"><select wire:model.live="sex" class="rounded-lg border border-slate-300 px-3 py-2.5 text-sm"><option value="">All sex classifications</option>@foreach($sexes as $item)<option value="{{ $item->value }}">{{ $item->label() }}</option>@endforeach</select><select wire:model.live="sector" class="rounded-lg border border-slate-300 px-3 py-2.5 text-sm"><option value="">All sectors</option>@foreach($sectors as $item)<option value="{{ $item->id }}">{{ $item->name }}</option>@endforeach</select><select wire:model.live="office" class="rounded-lg border border-slate-300 px-3 py-2.5 text-sm"><option value="">All offices</option>@foreach($offices as $item)<option value="{{ $item->id }}">{{ $item->name }}</option>@endforeach</select></div></div>
<div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200"><thead class="bg-slate-50"><tr><th class="px-5 py-3 text-left text-[10px] font-bold uppercase text-slate-500">Beneficiary</th><th class="px-5 py-3 text-left text-[10px] font-bold uppercase text-slate-500">Project</th><th class="px-5 py-3 text-left text-[10px] font-bold uppercase text-slate-500">Office / Province</th><th class="px-5 py-3 text-left text-[10px] font-bold uppercase text-slate-500">Sectors</th><th class="px-5 py-3 text-right text-[10px] font-bold uppercase text-slate-500">Action</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($beneficiaries as $beneficiary)<tr><td class="px-5 py-4"><p class="text-sm font-bold text-slate-900">{{ $beneficiary->fullName() }}</p><p class="mt-1 text-xs text-slate-500">{{ $beneficiary->reference_number ?: 'No reference' }} · {{ $beneficiary->sex?->label() ?? 'Unspecified' }}</p></td><td class="px-5 py-4 text-xs"><p class="font-semibold text-slate-800">{{ $beneficiary->project?->title }}</p><p class="mt-1 text-slate-500">{{ $beneficiary->project?->registry_number }}</p></td><td class="px-5 py-4 text-xs"><p>{{ $beneficiary->project?->office?->name ?? '—' }}</p><p class="mt-1 text-slate-500">{{ $beneficiary->project?->primaryLocation?->province?->name ?? '—' }}</p></td><td class="px-5 py-4"><div class="flex max-w-[300px] flex-wrap gap-1">@forelse($beneficiary->sectors as $sector)<x-ui.badge>{{ $sector->name }}</x-ui.badge>@empty<span class="text-xs text-slate-400">No sector tags</span>@endforelse</div></td><td class="px-5 py-4 text-right"><a href="{{ route('projects.beneficiaries',$beneficiary->project) }}" class="text-xs font-bold text-[#164b73]">Open Project</a></td></tr>@empty<tr><td colspan="5" class="px-5 py-12 text-center text-sm text-slate-500">No beneficiaries match the current filters.</td></tr>@endforelse</tbody></table></div>@if($beneficiaries->hasPages())<div class="border-t border-slate-100 px-5 py-4">{{ $beneficiaries->links() }}</div>@endif</x-ui.card></div>
